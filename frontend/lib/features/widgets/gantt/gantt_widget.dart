import 'package:flutter/material.dart';
class GanttWidget extends StatelessWidget {
  final Map<String, dynamic> data;
  const GanttWidget(this.data, {super.key});
  @override
  Widget build(BuildContext context) => const Center(child: Text("Gantt Chart Placeholder"));
}
