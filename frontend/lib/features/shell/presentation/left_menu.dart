import 'package:flutter/material.dart';
class LeftMenu extends StatelessWidget {
  const LeftMenu({super.key});
  @override
  Widget build(BuildContext context) => Container(width: 250, color: Colors.grey[200], child: const Center(child: Text("Menu")));
}
